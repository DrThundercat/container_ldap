version: "3.9"

services:
  ldap:
    image: docker.io/389ds/dirsrv:latest
    container_name: my-389ds
    environment:
      DS_DM_PASSWORD: appleapple
      DS_SUFFIX_NAME: o=farmer
    volumes:
      - dev_data:/data
    ports:
      - "1389:3389"   # host 1389 -> container 3389 (LDAP)
      - "1636:3636"   # host 1636 -> container 3636 (LDAPS)

volumes:
  dev_data:
